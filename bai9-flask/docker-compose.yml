version: "3.8"

services:
  flask:
    build: .
    container_name: bai9-flask
    ports:
      - "5000:5000"
    environment:
      FLASK_APP: app.py
      FLASK_ENV: development
    volumes:
      - ./:/app
